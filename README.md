# 🚀 Воронка анонсов — автоматическая рассылка персонализированных баннеров в Telegram

## Для кого этот проект

Для владельцев онлайн-школ, курсов, коучинговых программ, ретритов — всех, кто хочет автоматизировать рассылку красивых рекламных сообщений в Telegram без найма дизайнера.

---

## Какую задачу решает

**Проблема:** Каждый клиент требует персонализированного подхода. Создавать вручную сотни уникальных рекламных баннеров с именами, должностями и компаниями — долго и дорого.

**Решение:** Система автоматически генерирует профессиональные баннеры для каждого получателя и отправляет их через Telegram-бота. Вы тратите 5 минут на настройку вместо часов на дизайн.

---

## Как это работает

### Простыми словами:

1. **Загружаете список клиентов**  
   Обычная таблица (как Excel) с именами, должностями и Telegram ID

2. **Выбираете стиль оформления**  
   6 готовых профилей (Wellness, Корпоративный, Креативный, Люксовый, Технологичный, Минималистичный) или загружаете свой

3. **Система создаёт персонализированные баннеры**  
   Для каждого клиента генерируется 3 картинки:
   - **Этап 1: Интерес** — привлекает внимание к проблеме
   - **Этап 2: Решение** — показывает ваше предложение
   - **Этап 3: Дедлайн** — создаёт срочность действия

4. **Telegram-бот рассылает баннеры**  
   Каждый клиент получает изображение с кнопкой для перехода на ваш сайт

5. **Получаете статистику**  
   Система показывает, кому отправлено и какие варианты использованы

---

## A/B-тестирование — что это

Для каждого этапа воронки есть 3 варианта оформления (A, B, C). Система может:
- Отправлять **фиксированный вариант** каждому клиенту (указан в таблице)
- Отправлять **случайные варианты** для тестирования эффективности

Это помогает понять, какой дизайн и текст работают лучше.

---

## Примеры стилей оформления

| Профиль | Описание | Лучше всего для |
|---------|----------|-----------------|
| 🌿 **Wellness** | Спокойный, природный, мягкие тона | Коучинг, психология, йога, ретриты |
| 🏢 **Corporate** | Строгий, минималистичный | B2B, консалтинг, финансы |
| 🎨 **Creative** | Яркий, современный | Маркетинг, дизайн, стартапы |
| 👑 **Luxury** | Премиальный, тёмный | Элитные услуги, VIP-программы |
| 💻 **Tech** | Технологичный | IT, SaaS, AI-проекты |
| ⬜ **Minimal** | Чистый, много воздуха | Архитектура, фотография |

Каждый профиль включает:
- Свою цветовую палитру
- Уникальные шрифты
- Стиль кнопок и карточек
- Тон сообщений (формальный/дружелюбный/динамичный)

---

## Технологии

### Что используется и зачем

**Python 3.7+**  
Язык программирования, на котором написан весь проект. Простой в изучении и популярный для автоматизации.

**Telegram Bot API (библиотека aiogram)**  
Позволяет вашему боту отправлять сообщения в Telegram без ручной работы. Как почтовый сервис, только для мессенджера.

**html2image**  
Превращает текст и изображения в красивый баннер. Работает как "скриншот веб-страницы" — вы описываете дизайн, он создаёт картинку.

**pandas**  
Читает вашу таблицу с клиентами (CSV-файл) как программа Excel. Помогает обрабатывать сотни записей за секунды.

**PyYAML**  
Считывает настройки стиля из файлов-профилей. Это как конфигурационный файл для цветов, шрифтов и размеров.

**python-dotenv**  
Хранит токен вашего Telegram-бота в отдельном защищённом файле. Нужно для безопасности — токен не попадёт в публичный код.

---

## Что нужно для запуска

### Минимальные требования:

1. **Python версии 3.7 или новее**  
   Проверьте: откройте терминал и введите `python3 --version`

2. **Telegram-бот**  
   Создаётся бесплатно через @BotFather в Telegram за 2 минуты

3. **Список клиентов**  
   Таблица с 5 колонками: имя, должность, компания, Telegram ID, вариант теста

4. **5 минут времени**  
   На установку и первый запуск

---

## Быстрый старт

### Шаг 1: Установка

Скачайте проект и установите необходимые библиотеки. Откройте терминал и выполните команды:

```bash
git clone git@github.com:neyroseti25-cell/VM_36.git
cd VM_36
pip install -r requirements.txt
```

### Шаг 2: Создание Telegram-бота

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username)
4. Скопируйте токен (длинная строка вида `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 3: Настройка

Создайте файл `.env` в папке проекта и добавьте туда токен:

```
BOT_TOKEN=ваш_токен_здесь
```

### Шаг 4: Добавление клиентов

Откройте файл `users.csv` и заполните таблицу:

```
name,role,company,telegram_id,variant
Анна,Директор,ТехКомпания,123456789,a
Иван,Менеджер,Стартап,987654321,b
```

**Как узнать Telegram ID клиента:**
Отправьте сообщение боту @userinfobot — он покажет ваш ID.

### Шаг 5: Тестирование

Проверьте, что всё работает (создаст баннеры, но не отправит):

```bash
python3 bot_funnel.py --test
```

Если всё прошло успешно, в папке `output/` появятся картинки.

### Шаг 6: Отправка

Когда убедитесь, что баннеры выглядят хорошо, запустите рассылку:

```bash
python3 bot_funnel.py --send
```

---

## Структура воронки

### Этап 1: Интерес — привлечение внимания

**Задача:** Зацепить получателя эмоциональным посылом

- **Вариант A:** Фокус на глубине и гармонии  
  _"Откройте глубину в себе"_

- **Вариант B:** Работа с болью и безопасностью  
  _"Что скрыто в вашей душе?"_

- **Вариант C:** Прямой призыв к действию  
  _"Начните путь к себе"_

### Этап 2: Решение — предложение продукта

**Задача:** Показать, как вы решаете проблему

- **Вариант A:** Базовое предложение  
  5-10 сессий, отчёты с insights

- **Вариант B:** Упор на безопасность и аутентичность  
  Безопасное пространство для трансформации

- **Вариант C:** Осознанный подход  
  Natural harmony в каждом шаге

### Этап 3: Дедлайн — создание срочности

**Задача:** Мотивировать к немедленному действию

- **Вариант A:** Ограниченные места  
  "Только 10 мест! От 25 000 руб."

- **Вариант B:** VIP-доступ и обратный отсчёт  
  "Эксклюзивное предложение истекает"

- **Вариант C:** Персональный призыв  
  "{{name}}, это ваш шанс на гармонию"

---

## Создание своего стиля

Если готовые профили не подходят, создайте собственный:

1. Скопируйте файл `profiles/custom.yaml`
2. Откройте его в любом текстовом редакторе
3. Измените цвета, шрифты, размеры логотипа
4. Сохраните под новым именем (например, `mybrand.yaml`)
5. Запустите с новым профилем

Каждый профиль позволяет настроить:
- Логотип (текстовый или изображение)
- 12+ цветов (фон, текст, кнопки, тени)
- Шрифты и их размеры
- Размеры изображений (квадрат, широкий, stories)
- Стиль кнопок (скругление, тени)
- Тон сообщений (формальный/дружелюбный)
- Контакты и соцсети

---

## Примеры использования

### Тестирование с разными стилями

```bash
python3 bot_funnel.py --test --brand wellness
python3 bot_funnel.py --test --brand corporate
python3 bot_funnel.py --test --brand luxury
```

### Отправка с определённым стилем

```bash
python3 bot_funnel.py --send --brand creative
```

### Случайное A/B-тестирование

```bash
python3 bot_funnel.py --send --variant random
```

### Просмотр доступных профилей

```bash
python3 bot_funnel.py --list-profiles
```

---

## Что получается на выходе

### При тестировании:

- **15 картинок-баннеров** (5 клиентов × 3 этапа воронки)
- **Размер:** 1200×900 пикселей (оптимально для Telegram)
- **Формат:** PNG с высоким качеством
- **Персонализация:** Каждый баннер содержит имя, должность и компанию клиента
- **Брендинг:** Ваш логотип, цвета и стиль на всех изображениях

### При отправке:

- Каждый клиент получает 3 сообщения с картинками
- Под каждым баннером — кнопка для перехода на ваш сайт
- В конце — статистика: кому отправлено, какие варианты показаны
- Автоматическая обработка ошибок (если клиент заблокировал бота)

---

## Возможные проблемы и решения

### Ошибка: "BOT_TOKEN не найден"

**Причина:** Не создан файл `.env` с токеном бота

**Решение:** Создайте файл `.env` в папке проекта и добавьте токен

### Ошибка при отправке: "User is deactivated"

**Причина:** Клиент не добавил вашего бота в контакты

**Решение:** Попросите клиента найти бота в Telegram и нажать "Start"

### Нет картинок в папке output/

**Причина:** Нет прав на запись файлов

**Решение:** Проверьте права доступа к папке проекта

### Баннеры выглядят некорректно

**Причина:** Не установлены системные зависимости для html2image

**Решение на macOS:**
```bash
brew install chromium
```

**Решение на Ubuntu/Debian:**
```bash
sudo apt-get install chromium-browser
```

### Логотип не отображается

**Причина:** Неверный путь к файлу логотипа в профиле

**Решение:** Убедитесь, что файл логотипа находится в папке `assets/logos/` и путь указан правильно в YAML-файле профиля

---

## Структура проекта

```
VM36/
├── bot_funnel.py          # Основной скрипт (запуск рассылки)
├── config.py              # Настройки и загрузка профилей
├── utils.py               # Вспомогательные функции
├── users.csv              # Ваш список клиентов
├── .env                   # Токен бота (создаётся вручную)
├── requirements.txt       # Список библиотек для установки
│
├── profiles/              # 📁 Готовые стили оформления
│   ├── wellness.yaml
│   ├── corporate.yaml
│   ├── creative.yaml
│   ├── luxury.yaml
│   ├── tech.yaml
│   ├── minimal.yaml
│   └── custom.yaml        # Шаблон для вашего стиля
│
├── templates/             # 📁 Шаблоны дизайна (9 вариантов)
│   ├── interest_a.html    # Этап интереса — вариант A
│   ├── interest_b.html    # Этап интереса — вариант B
│   ├── interest_c.html    # Этап интереса — вариант C
│   ├── solution_a.html    # Этап решения — вариант A
│   ├── solution_b.html    # Этап решения — вариант B
│   ├── solution_c.html    # Этап решения — вариант C
│   ├── deadline_a.html    # Этап дедлайна — вариант A
│   ├── deadline_b.html    # Этап дедлайна — вариант B
│   ├── deadline_c.html    # Этап дедлайна — вариант C
│   └── styles.css         # Базовые стили
│
├── assets/                # 📁 Изображения (логотипы, иконки)
│   ├── logos/
│   ├── icons/
│   └── patterns/
│
├── output/                # 📁 Сгенерированные баннеры (создаётся автоматически)
│
└── test_basic.py          # Проверка структуры проекта
```

---

## Лицензия

MIT License — используйте свободно для коммерческих и личных целей.

---

## Контакты и поддержка

Если возникли вопросы или нужна помощь с настройкой — создайте Issue в GitHub-репозитории проекта.

---

## Для разработчиков

### Запуск тестов

Базовая проверка структуры:

```bash
python3 test_basic.py
```

### Зависимости проекта (requirements.txt)

```
aiogram==3.13.1      # Telegram Bot API
pandas               # Работа с CSV
jinja2               # Шаблонизация HTML
html2image           # Генерация PNG из HTML
PyYAML               # Чтение YAML профилей
python-dotenv        # Загрузка переменных окружения
Pillow               # Обработка изображений
```

### Системные зависимости (опционально)

Для WeasyPrint (если решите использовать вместо html2image):

**macOS:**
```bash
brew install cairo pango gdk-pixbuf libffi
```

**Ubuntu/Debian:**
```bash
sudo apt-get install libcairo2-dev libpango1.0-dev libgdk-pixbuf2.0-dev libffi-dev
```

### Формат CSV файла

Обязательные поля:

| Поле | Тип | Описание |
|------|-----|----------|
| `name` | Текст | Имя клиента |
| `role` | Текст | Должность |
| `company` | Текст | Название компании |
| `telegram_id` | Число | ID чата в Telegram |
| `variant` | a/b/c | Вариант для A/B-тестирования |

### API функций

**load_users(csv_path)** — загружает клиентов из CSV  
**render_html(template, data, profile)** — рендерит HTML из шаблона  
**html_to_png(html, output_path, profile)** — генерирует PNG  
**load_brand_profile(name)** — загружает профиль брендинга  
**get_image_size(profile)** — возвращает размеры из профиля

---

**Версия:** 1.0  
**Последнее обновление:** 18 января 2026
