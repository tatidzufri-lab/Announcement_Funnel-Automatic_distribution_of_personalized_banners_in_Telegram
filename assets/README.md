# 📁 Assets — Медиа-ресурсы

Папка для хранения логотипов, иконок и паттернов, используемых в баннерах.

## 📂 Структура

```
assets/
├── logos/              # Логотипы брендов
│   ├── poznay_sebya.svg      # Светлая версия (для светлых фонов)
│   └── poznay_sebya_dark.svg # Тёмная версия (для тёмных фонов)
├── icons/              # Иконки для фич и маркеров
│   ├── check.svg       # Галочка
│   ├── star.svg        # Звезда
│   └── arrow.svg       # Стрелка
├── patterns/           # Фоновые паттерны
│   └── dots.svg        # Точечный паттерн
└── README.md           # Этот файл
```

## 🎨 Использование в профилях

### Логотип

В файле профиля (`profiles/wellness.yaml`):

```yaml
brand:
  logo:
    # Путь к изображению логотипа
    image: "assets/logos/poznay_sebya.svg"
    image_dark: "assets/logos/poznay_sebya_dark.svg"
    
    # Размеры
    image_width: 200
    image_height: 50
    
    # Текстовый fallback (если изображение не найдено)
    text: "POZNAY SEBYA"
    subtitle: "KNOW YOURSELF"
```

### Иконки

```yaml
icons:
  feature_marker: "assets/icons/star.svg"  # или emoji: "✦"
  check_marker: "assets/icons/check.svg"   # или emoji: "✓"
```

## 📐 Рекомендации

### Логотипы

| Параметр | Рекомендация |
|----------|--------------|
| Формат | SVG (предпочтительно) или PNG |
| Размер SVG | viewBox 400x80 или подобный |
| Размер PNG | 400x100 px, @2x для Retina |
| Прозрачность | Да, для PNG |
| Версии | Светлая + Тёмная |

### Иконки

| Параметр | Рекомендация |
|----------|--------------|
| Формат | SVG |
| Размер | viewBox 24x24 |
| Стиль | Outline или Filled |
| Цвета | Используйте currentColor или цвета из профиля |

### Паттерны

| Параметр | Рекомендация |
|----------|--------------|
| Формат | SVG с pattern |
| Opacity | 0.05-0.15 для ненавязчивости |
| Размер тайла | 20-50px |

## 🔄 Как добавить свой логотип

1. Поместите файл в `assets/logos/`
2. Обновите путь в профиле:

```yaml
brand:
  logo:
    image: "assets/logos/my_logo.svg"
    image_width: 180
    image_height: 45
```

3. Запустите тест:

```bash
python3 bot_funnel.py --test --brand wellness
```

## ⚠️ Важно

- Пути к assets **относительные** от корня проекта
- При рендеринге они автоматически преобразуются в абсолютные
- SVG предпочтительнее PNG для качества на любом разрешении
